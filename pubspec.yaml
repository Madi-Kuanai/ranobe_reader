name: ranobe_reader
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"


dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  shared_preferences: ^2.0.15
  provider: ^6.0.3
  html:
  http: ^0.13.4
  google_fonts: ^3.0.1
  auto_size_text: ^3.0.0
  palette_generator: ^0.3.3+1
  internet_file: ^1.2.0
  epub_view: ^3.0.0
  lottie: ^1.4.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  dio: ^4.0.6
  percent_indicator: ^4.0.1


dev_dependencies:
  hive_generator: ^0.7.0+2
  build_runner: ^1.8.1
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/json/
    - assets/animations/loadingAnimation.json
    - assets/zapisi_hostov_i_igrokov.epub
